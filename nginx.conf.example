server {
    listen 80;
    # 替换为您的域名
    server_name www.code.lookmori.cn;
    
    # 访问日志
    access_log /www/wwwlogs/www.code.lookmori.cn.log;
    error_log /www/wwwlogs/www.code.lookmori.cn.error.log;
    
    # 根目录，替换为实际的项目路径
    root /www/wwwroot/www.code.lookmori.cn;
    
    # 反向代理到Node.js应用
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
    
    # 静态资源缓存
    location /_next/static/ {
        alias /www/wwwroot/www.code.lookmori.cn/.next/static/;
        expires 30d;
        access_log off;
        add_header Cache-Control "public, max-age=2592000, immutable";
    }
    
    location /public/ {
        alias /www/wwwroot/www.code.lookmori.cn/public/;
        expires 30d;
        access_log off;
        add_header Cache-Control "public, max-age=2592000";
    }
    
    # 安全设置
    # 防止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # 禁止访问敏感目录
    location ~ ^/(src|node_modules|scripts)/ {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# HTTPS配置示例(启用SSL后使用)
# server {
#     listen 443 ssl http2;
#     server_name www.code.lookmori.cn;
#     
#     # SSL证书配置(宝塔面板会自动生成)
#     ssl_certificate    /www/server/panel/vhost/cert/www.code.lookmori.cn/fullchain.pem;
#     ssl_certificate_key    /www/server/panel/vhost/cert/www.code.lookmori.cn/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # 访问日志
#     access_log /www/wwwlogs/www.code.lookmori.cn.log;
#     error_log /www/wwwlogs/www.code.lookmori.cn.error.log;
#     
#     # 根目录
#     root /www/wwwroot/www.code.lookmori.cn;
#     
#     # 反向代理配置
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#     
#     # 其他配置与HTTP相同...
# }
# 
# # HTTP请求重定向到HTTPS
# server {
#     listen 80;
#     server_name www.code.lookmori.cn;
#     return 301 https://$host$request_uri;
# } 