---
title: Python基础入门
description: 学习Python的基本语法、数据类型和控制结构
date: '2023-05-07'
tags: ['python', '基础', '入门']
level: '初级'
order: 1
---

# Python基础入门

Python是一种简单易学且功能强大的编程语言。它具有高效的高级数据结构和简单有效的面向对象编程方法。Python优雅的语法和动态类型及其解释性使其成为大多数平台上许多领域的脚本编写和快速应用程序开发的理想语言。

## 1. Python环境设置

要开始使用Python，您需要在计算机上安装Python解释器。您可以从[Python官方网站](https://www.python.org/downloads/)下载最新版本。

安装后，您可以通过在命令行中输入以下命令来验证安装：

```bash
python --version
# 或
python3 --version
```

## 2. 你好，世界！

让我们从经典的"Hello, World!"程序开始：

```python
print("Hello, World!")
```

将此代码保存在名为`hello.py`的文件中，并使用以下命令运行它：

```bash
python hello.py
# 或
python3 hello.py
```

## 3. Python数据类型

Python有几种内置数据类型：

### 数字
```python
# 整数
x = 5
# 浮点数
y = 3.14
# 复数
z = 1 + 2j
```

### 字符串
```python
name = "Python"
multiline = """这是一个
多行字符串"""
```

### 列表
```python
fruits = ["apple", "banana", "cherry"]
# 访问元素
print(fruits[0])  # 输出: apple
# 修改元素
fruits[0] = "orange"
```

### 元组（不可变列表）
```python
coordinates = (10, 20)
# 尝试修改会导致错误
# coordinates[0] = 15  # 错误!
```

### 字典
```python
person = {
    "name": "张三",
    "age": 30,
    "city": "北京"
}
# 访问值
print(person["name"])  # 输出: 张三
```

### 集合
```python
fruits = {"apple", "banana", "cherry"}
# 添加元素
fruits.add("orange")
```

## 4. 控制流

### 条件语句
```python
x = 10
if x > 5:
    print("x大于5")
elif x == 5:
    print("x等于5")
else:
    print("x小于5")
```

### 循环
```python
# for循环
for i in range(5):
    print(i)  # 输出0到4

# while循环
count = 0
while count < 5:
    print(count)
    count += 1
```

## 5. 函数

```python
def greet(name):
    """这是一个简单的问候函数"""
    return f"你好，{name}！"

# 调用函数
message = greet("世界")
print(message)  # 输出: 你好，世界！
```

## 6. 异常处理

```python
try:
    # 可能引发异常的代码
    result = 10 / 0
except ZeroDivisionError:
    # 处理特定异常
    print("不能除以零！")
except Exception as e:
    # 处理其他异常
    print(f"发生错误: {e}")
finally:
    # 无论是否发生异常都会执行
    print("异常处理完成")
```

## 下一步

恭喜！您已经了解了Python的基础知识。在下一个教程中，我们将探讨更多高级主题，如模块、包和面向对象编程。 